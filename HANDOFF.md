# HANDOFF — обмін між інструментами

Три інструменти працюють над цим репозиторієм.

| Скорочення | Що це | Що вміє |
|---|---|---|
| **ЧАТ** | claude.ai, проєкт STARPaw | читає і **пише** в репозиторій напряму |
| **ДИЗАЙН** | claude.ai/design | читає репозиторій через Import, **писати не вміє** |
| **КОД** | claude.ai/code | читає і пише, але тільки в робочу гілку через PR |

## Що який інструмент читає автоматично

Це найважливіше в усьому файлі.

| Файл | ДИЗАЙН | КОД |
|---|---|---|
| `CLAUDE.md` | **ні** | так, автоматично |
| `DESIGN.md` | так, автоматично | ні |
| `SPEC.md` | ні | ні |
| `HANDOFF.md` | ні | ні |

**ДИЗАЙН не читає `CLAUDE.md`.** Тому правила, які мають зв'язувати дизайн,
дублюються коротким блоком на початку `DESIGN.md`.

Усе, що не читається автоматично, треба відкривати вручну одним рядком у
новому чаті. Наприклад:

> Read HANDOFF.md, section "ДЛЯ ДИЗАЙНУ", and do the open items.

---

# Шість напрямків

## 1. ЧАТ → КОД

ЧАТ дописує пункт у `# ДЛЯ КОДУ` і комітить у `main`.
Людина відкриває claude.ai/code, обирає репозиторій, пише:

> Read HANDOFF.md and do the open items in "ДЛЯ КОДУ".

КОД робить, відкриває PR, переносить пункт у `# ЗРОБЛЕНО`.
Копіпасту немає.

## 2. ЧАТ → ДИЗАЙН

ЧАТ дописує пункт у `# ДЛЯ ДИЗАЙНУ` і комітить у `main`.
Людина відкриває проєкт у claude.ai/design:

- **перший раз** — `Import → From GitHub`, обрати репозиторій (це також
  встановлює застосунок `claude-design-import`);
- **далі** — просто відкрити той самий проєкт.

Потім пише один рядок з посиланням на секцію.
Копіпаст: один рядок. Сам бриф лежить у репозиторії.

## 3. ДИЗАЙН → ЧАТ

**Прямого шляху немає.** ДИЗАЙН не пише в репозиторій, ЧАТ не бачить канвас.

Маршрут в обхід: ДИЗАЙН → КОД → репозиторій → ЧАТ.
Спершу віддати роботу в КОД, КОД комітить, після цього ЧАТ бачить результат.

Якщо треба обговорити рішення в чаті до того, як воно стало кодом — людина
кидає в ЧАТ скріншот канвасу або копіює текстове пояснення. Це єдиний
випадок, коли копіпаст неминучий.

## 4. ДИЗАЙН → КОД

На канвасі: `Export → Handoff to Claude Code → Send to Claude Code Web`.
У КОДі з'являється сесія з макетом. Людина пише:

> Apply this to the repo. Follow the file structure in SPEC.md.
> Do not add build tools, frameworks or dependencies.

КОД оновлює `DESIGN.md` і файли в `assets/css/`, відкриває PR.
Це кнопка, не копіпаст.

## 5. КОД → ЧАТ

КОД дописує в `# ЗРОБЛЕНО` що зроблено, і в `# ПИТАННЯ` те, чого не вирішив.
Людина в ЧАТі пише: `перевір репо`. ЧАТ читає файли й PR напряму.
Копіпасту немає.

## 6. КОД → ДИЗАЙН

КОД комітить `DESIGN.md` і CSS у `main`.
У проєкті ДИЗАЙНУ людина повторює `Import → From GitHub`.

**Синхронізація не автоматична.** Поки не переімпортуєш — ДИЗАЙН малює за
старою версією. Робити щоразу, коли КОД змінив `DESIGN.md` або CSS.

---

# Загальні правила

1. Ніщо не їде саме собою, крім читання репозиторію. Кожен перехід між
   інструментами робить людина: або кнопкою, або одним рядком.
2. Новий чат нічого не пам'ятає. Увесь контекст живе в репозиторії.
3. Завдання формулюється так, щоб його зрозумів той, хто бачить проєкт
   уперше. Без «як ми домовлялись».
4. Один пункт — одна задача — один PR.
5. Ніякої збірки, npm, залежностей і фреймворків. Це правило не обговорює
   жоден з трьох інструментів.
6. Усе тільки в браузері. Ніякого термінала і локальної машини.

---

# ДЛЯ КОДУ

_(порожньо)_

# ДЛЯ ДИЗАЙНУ

_(порожньо)_

# ПИТАННЯ

## Cloudflare Pages: "Could not detect a directory containing static files"

Білд Cloudflare Pages падає з помилкою wrangler, бо публікувати поки
нічого: у репозиторії немає жодного `*.html` файлу, немає `assets/**`,
і немає `SPEC.md` та `DESIGN.md`, за якими КОД мав би їх створити.
Є лише довідкові документи (`DESIGNtemplateproposal.md`,
`firstversioncontent.md`, `source/`).

Per CLAUDE.md КОД не вигадує структуру сторінок і не створює розділи без
прямої вказівки. Секція `# ДЛЯ КОДУ` тут же — порожня.

Щоб з'явився хоч один HTML-файл і сайт почав публікуватись, потрібно:

1. ЧАТ створює `SPEC.md` (маршрути, тексти, структура сторінок) і
   `DESIGN.md` (кольори, шрифти, компоненти);
2. ЧАТ дописує конкретну задачу в `# ДЛЯ КОДУ` (наприклад: "зробити
   головну сторінку за SPEC.md").

Без цього КОД не буде вгадувати структуру сайту.

# ЗРОБЛЕНО

_(порожньо)_
